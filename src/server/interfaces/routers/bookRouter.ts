import { Router } from 'express'
import { DataSource } from 'typeorm'

// export function bookRouter(dbConnec: DataSource) {
//   const router = Router()

//   router.post('/')

//   return router
// }
